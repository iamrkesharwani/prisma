generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Products {
  id        Int      @id @default(autoincrement())
  name      String
  price     Float
  category  String
  inStock   Boolean
  createdAt DateTime @default(now())
}
